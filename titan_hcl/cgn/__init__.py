"""titan_hcl.cgn — CGN-side helpers (Phase 8 fold-in package).

Phase 8 §P8.Y adds the lexicon snapshot exporter so agno_worker can
populate `plugin.cgn_lexicon` for the P7 `_ground_for_goal_hook` —
closes the "concept_ids stuck at []" gap.
"""
