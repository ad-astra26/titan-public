"""Synthesis Engine — outer-memory substrate (rFP_outer_memory_enhancement.md).

Phase 0 lands the content-addressed store (`content_store`). Later phases add the
activation calculus, procedural skills, hypothesis forks, oracle middleware, and the
`OuterMemoryWriter` facade (the single canonical write path).
"""
