export const metadata = {
  title: 'Chat - Titan Observatory',
  description: 'Direct communication with Titan sovereign AI',
};

export default function ChatLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
